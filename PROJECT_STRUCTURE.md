# 📋 SinoSEA-Vite 项目结构说明

## 📖 目录

- [项目概述](#项目概述)
- [目录结构](#目录结构)
- [核心文件说明](#核心文件说明)
- [功能模块详解](#功能模块详解)
- [架构设计理念](#架构设计理念)

---

## 📌 项目概述

**SinoSEA-Vite** 是一个基于 React + Vite + TypeScript 的现代化Web应用。

### 技术栈

| 技术          | 版本    | 用途       |
| ------------- | ------- | ---------- |
| React         | 19.2.0  | UI框架     |
| Vite          | 7.2.4   | 构建工具   |
| TypeScript    | 5.9.3   | 类型检查   |
| React Router  | 7.12.0  | 路由管理   |
| Tailwind CSS  | 4.1.18  | 样式框架   |
| i18next       | 25.8.0  | 国际化     |
| Axios         | 1.13.2  | HTTP客户端 |
| Framer Motion | 12.29.0 | 动画库     |

---

## 📌 架构设计

本项目采用 **功能导向 (Feature-Based)** 的架构设计，按业务功能模块组织代码，而非按技术类型分类。

### 核心理念

- ✅ **高内聚低耦合** - 每个功能模块包含该功能的所有资源
- ✅ **清晰的分层** - app层（应用）、features层（业务）、shared层（共享）
- ✅ **统一导出** - 每个模块提供index.ts统一导出接口
- ✅ **便于维护** - 功能内聚，改动不影响其他模块
- ✅ **易于扩展** - 新增功能只需添加新feature目录

---

## 📂 项目完整目录结构

```
SinoSEA-Vite/
├── 📄 核心配置文件
│   ├── package.json              # 项目依赖和脚本配置
│   ├── pnpm-lock.yaml            # 依赖版本锁定文件
│   ├── tsconfig.json             # TypeScript通用配置
│   ├── tsconfig.app.json         # 应用TypeScript配置
│   ├── tsconfig.node.json        # Node环境TypeScript配置
│   ├── vite.config.ts            # Vite构建配置
│   ├── eslint.config.js          # ESLint代码检查配置
│   ├── wrangler.jsonc            # Wrangler配置文件
│   ├── index.html                # HTML入口文件
│   └── PROJECT_STRUCTURE.md      # 项目结构说明文档
│
├── 🎨 public/                    # 公共静态资源
│   └── images/                   # 图片资源目录
│       ├── chat-bg.avif          # 聊天背景图
│       ├── landingbg.avif        # 首页背景图
│       ├── login-bg.avif         # 登录背景图
│       └── team/                 # 团队图片目录
│
└── 💻 src/                       # 源代码目录
    ├── app/                      # ⚙️ 应用核心层
    │   ├── App.tsx               # 应用主组件（路由配置）
    │   ├── main.tsx              # 应用入口文件
    │   ├── tailwind.config.js    # Tailwind CSS配置
    │   ├── layouts/
    │   │   └── MainLayout.tsx    # 主布局组件
    │   └── styles/               # 全局样式
    │       ├── App.css           # App样式
    │       └── index.css         # 全局样式和CSS变量
    │
    ├── features/                 # 🎯 功能模块层（业务特性）
    │   │
    │   ├── auth/                 # 🔐 认证模块
    │   │   ├── pages/            # 页面组件
    │   │   │   ├── Login.tsx     # 登录页面
    │   │   │   ├── Register.tsx  # 注册页面
    │   │   │   └── ForgotPassword.tsx  # 忘记密码页面
    │   │   ├── components/       # 私有组件（仅在此模块使用）
    │   │   │   ├── LoginErrorModal.tsx # 登录错误弹窗
    │   │   │   └── LoginBackground.tsx # 登录背景组件
    │   │   ├── layouts/
    │   │   │   └── AuthLayout.tsx      # 认证布局组件
    │   │   └── index.ts          # 统一导出接口
    │   │
    │   ├── chat/                 # 💬 聊天模块
    │   │   ├── pages/
    │   │   │   └── Chat.tsx      # 聊天页面
    │   │   ├── components/       # 私有聊天组件
    │   │   │   ├── ChatWindow.tsx    # 聊天窗口
    │   │   │   ├── MessageBubble.tsx # 消息气泡
    │   │   │   ├── Sidebar.tsx       # 侧边栏
    │   │   │   └── TopNav.tsx        # 顶部导航
    │   │   ├── types/
    │   │   │   └── chat.types.ts # 聊天相关类型定义
    │   │   └── index.ts
    │   │
    │   ├── landing/              # 🏠 首页模块
    │   │   ├── pages/
    │   │   │   └── Landing.tsx   # 首页页面
    │   │   ├── components/
    │   │   │   └── HomeBackground.tsx # 首页背景
    │   │   └── index.ts
    │   │
    │   ├── about/                # 👥 关于我们模块
    │   │   ├── pages/
    │   │   │   └── AboutUs.tsx   # 关于页面
    │   │   ├── components/
    │   │   │   ├── AboutHeader.tsx   # 关于页头部
    │   │   │   ├── AboutContent.tsx  # 关于页内容
    │   │   │   └── AboutFooter.tsx   # 关于页底部
    │   │   └── index.ts
    │   │
    │   ├── complaint/            # 📬 投诉模块
    │   │   ├── pages/
    │   │   │   └── ComplaintPage.tsx # 投诉页面
    │   │   ├── components/
    │   │   │   ├── ComplaintHeader.tsx    # 投诉页头
    │   │   │   ├── ComplaintForm.tsx      # 投诉表单
    │   │   │   └── ComplaintTopNav.tsx    # 投诉导航
    │   │   ├── types/
    │   │   │   └── complaint.types.ts # 投诉类型定义
    │   │   └── index.ts
    │   │
    │   ├── privacy/              # 🔒 隐私政策模块
    │   │   ├── pages/
    │   │   │   └── PrivacyPolicy.tsx # 隐私政策页面
    │   │   ├── components/
    │   │   │   ├── PolicyHeader.tsx  # 策略页头
    │   │   │   ├── PolicySection.tsx # 策略分段
    │   │   │   ├── PolicyTable.tsx   # 策略表格
    │   │   │   ├── Footer.tsx        # 页脚
    │   │   │   └── BackSection.tsx   # 返回区域
    │   │   ├── data/
    │   │   │   └── policyContent.tsx # 隐私政策内容
    │   │   └── index.ts
    │   │
    │   └── terms/                # ⚖️ 使用条款模块
    │       ├── pages/
    │       │   └── TermsOfUse.tsx # 使用条款页面
    │       └── index.ts
    │
    ├── shared/                   # 🔧 共享资源层（跨模块使用）
    │   │
    │   ├── components/           # 通用UI组件
    │   │   ├── LanguageSwitcher.tsx # 语言切换器
    │   │   ├── modals/           # 通用弹窗组件
    │   │   │   ├── LogoutConfirmModal.tsx    # 登出确认
    │   │   │   ├── SuccessToastModal.tsx     # 成功提示
    │   │   │   └── AvatarEditorModal.tsx     # 头像编辑
    │   │   ├── menus/            # 菜单组件
    │   │   │   └── UserAvatarMenu.tsx # 用户头像菜单
    │   │   └── index.ts          # 统一导出
    │   │
    │   ├── api/                  # API通信层
    │   │   ├── config.ts         # API端点配置和基础设置
    │   │   ├── chatSSE.ts        # SSE流处理（服务端推送）
    │   │   └── index.ts
    │   │
    │   ├── i18n/                 # 国际化配置
    │   │   ├── config.ts         # i18next配置
    │   │   ├── index.ts
    │   │   └── locales/          # 国际化语言文件
    │   │       ├── en-US/        # 英文翻译
    │   │       │   ├── auth.json     # 认证相关翻译
    │   │       │   ├── common.json   # 通用翻译
    │   │       │   └── landing.json  # 首页翻译
    │   │       └── zh-CN/        # 中文翻译
    │   │           ├── auth.json
    │   │           ├── common.json
    │   │           └── landing.json
    │   │
    │   ├── types/                # 通用类型定义
    │   │   ├── user.types.ts     # 用户相关类型
    │   │   └── index.ts
    │   │
    │   ├── utils/                # 工具函数
    │   │   ├── jwt.ts            # JWT令牌处理
    │   │   ├── env.ts            # 环境变量处理
    │   │   └── index.ts
    │   │
    │   └── data/                 # 静态数据
    │       └── projectIntro.ts   # 项目介绍数据
    │
    └── assets/                   # 💾 本地资源文件
        └── fonts/                # 字体文件目录

```

---

## 📄 核心文件说明

### 根目录配置文件

| 文件                 | 说明                          | 详细说明                                                                                  |
| -------------------- | ----------------------------- | ----------------------------------------------------------------------------------------- |
| `package.json`       | 项目元数据、依赖声明、NPM脚本 | 包含项目基本信息（名称、版本）、所有npm依赖版本、可执行脚本（dev、build、lint、format等） |
| `pnpm-lock.yaml`     | 包管理器锁定文件              | 锁定所有依赖的精确版本，确保团队成员安装的依赖版本完全一致，防止版本差异导致的问题        |
| `tsconfig.json`      | TypeScript基础配置            | 全局TS配置，定义编译选项、目标版本、模块系统等，被app和node配置继承                       |
| `tsconfig.app.json`  | 应用代码TS配置                | 应用代码专用配置，继承tsconfig.json，指定src目录、include/exclude规则等                   |
| `tsconfig.node.json` | Node脚本TS配置                | vite.config.ts等脚本文件的TS配置，编译目标为ES2020                                        |
| `vite.config.ts`     | Vite构建工具配置              | 配置开发服务器、构建输出、插件（React、TypeScript等）、路径别名、环境变量处理             |
| `eslint.config.js`   | 代码检查规则配置              | ESLint检查规则定义，包含JS和TS检查规则，确保代码质量和风格一致                            |
| `index.html`         | HTML入口文件                  | 应用主HTML文件，包含根DOM元素`<div id="root">`，引入`src/app/main.tsx`入口脚本            |
| `wrangler.jsonc`     | Cloudflare Workers配置        | 用于Cloudflare Workers部署配置，定义边缘计算相关参数                                      |

### 主要源代码文件

| 文件                 | 位置               | 说明                 | 主要内容                                                                       |
| -------------------- | ------------------ | -------------------- | ------------------------------------------------------------------------------ |
| `main.tsx`           | `src/app/`         | React应用入口点      | 初始化React应用，挂载到DOM，配置全局providers（路由、国际化、主题等）          |
| `App.tsx`            | `src/app/`         | 根组件，配置路由     | 定义应用的主路由结构，将7个feature模块的页面映射到对应路由路径                 |
| `MainLayout.tsx`     | `src/app/layouts/` | 应用主布局框架       | 定义全局布局结构（头部导航、侧边栏、主内容区、页脚等），为所有页面提供统一框架 |
| `tailwind.config.js` | `src/app/`         | Tailwind CSS样式配置 | 定义设计系统（颜色、字体、间距、圆角等），扩展Tailwind的默认配置               |
| `index.css`          | `src/app/styles/`  | 全局样式和CSS变量    | 包含全局CSS重置、根级CSS变量定义、页面级样式、响应式断点设置等                 |
| `App.css`            | `src/app/styles/`  | App组件样式          | App根组件的特定样式，包含全局布局相关样式                                      |

---

## 🎯 功能模块详解

### 1. **Auth 认证模块** (`src/features/auth/`)

**用途**: 用户身份验证和账户管理

**核心文件说明：**

| 文件                             | 类型     | 说明                                                                |
| -------------------------------- | -------- | ------------------------------------------------------------------- |
| `pages/Login.tsx`                | 页面组件 | 用户登录页面，包含邮箱/密码输入框、登录按钮、忘记密码链接、注册链接 |
| `pages/Register.tsx`             | 页面组件 | 用户注册页面，表单验证，创建新账户流程                              |
| `pages/ForgotPassword.tsx`       | 页面组件 | 密码重置页面，邮箱验证、重置令牌验证、新密码设置                    |
| `components/LoginErrorModal.tsx` | 子组件   | 登录错误提示弹窗，显示验证或服务器错误信息                          |
| `components/LoginBackground.tsx` | 子组件   | 登录页背景装饰组件，背景图片或渐变展示                              |
| `layouts/AuthLayout.tsx`         | 布局组件 | 认证模块统一布局，左右分栏设计（通常左侧背景图、右侧表单）          |
| `index.ts`                       | 导出文件 | 统一导出此模块的所有可公开使用的组件和页面                          |

**路由**: `/login`, `/register`, `/forgot-password`

---

### 2. **Chat 聊天模块** (`src/features/chat/`)

**用途**: 实时聊天功能

**核心文件说明：**

| 文件                           | 类型     | 说明                                                                          |
| ------------------------------ | -------- | ----------------------------------------------------------------------------- |
| `pages/Chat.tsx`               | 页面组件 | 聊天主页面，整合ChatWindow和Sidebar，管理聊天状态和消息列表                   |
| `components/ChatWindow.tsx`    | 子组件   | 聊天窗口容器，展示消息列表、输入框、发送按钮                                  |
| `components/MessageBubble.tsx` | 子组件   | 单条消息气泡组件，区分用户消息和AI回复，支持markdown渲染                      |
| `components/Sidebar.tsx`       | 子组件   | 聊天列表侧边栏，显示历史对话、搜索、新建对话按钮                              |
| `components/TopNav.tsx`        | 子组件   | 聊天页顶部导航，显示当前对话标题、设置按钮、菜单选项                          |
| `types/chat.types.ts`          | 类型文件 | TypeScript类型定义：Message（消息）、Conversation（对话）、User（用户）等接口 |
| `index.ts`                     | 导出文件 | 统一导出聊天模块组件                                                          |

**路由**: `/chat`

---

### 3. **Landing 首页模块** (`src/features/landing/`)

**用途**: 应用首页展示

**核心文件说明：**

| 文件                            | 类型     | 说明                                                                  |
| ------------------------------- | -------- | --------------------------------------------------------------------- |
| `pages/Landing.tsx`             | 页面组件 | 首页主体组件，整合Hero区域、功能展示、号召行动（CTA）按钮、用户评价等 |
| `components/HomeBackground.tsx` | 子组件   | 首页背景装饰，支持高清背景图片展示、渐变效果或视差滚动                |
| `index.ts`                      | 导出文件 | 统一导出首页模块                                                      |

**路由**: `/`

---

### 4. **About 关于模块** (`src/features/about/`)

**用途**: 公司/产品信息展示

**核心文件说明：**

| 文件                          | 类型     | 说明                                                         |
| ----------------------------- | -------- | ------------------------------------------------------------ |
| `pages/AboutUs.tsx`           | 页面组件 | 关于页面主体，整合页面各个分段组件，管理布局和内容流         |
| `components/AboutHeader.tsx`  | 子组件   | 页面标题区域，展示品牌logo、标题、副标题和简短介绍           |
| `components/AboutContent.tsx` | 子组件   | 主要内容区，包含公司使命、愿景、团队背景、发展历程等核心信息 |
| `components/AboutFooter.tsx`  | 子组件   | 页脚区域，包含联系方式、社交媒体链接、合作伙伴列表等         |
| `index.ts`                    | 导出文件 | 统一导出关于模块                                             |

**路由**: `/about`

---

### 5. **Complaint 投诉模块** (`src/features/complaint/`)

**用途**: 用户投诉和反馈

**核心文件说明：**

| 文件                             | 类型     | 说明                                                                         |
| -------------------------------- | -------- | ---------------------------------------------------------------------------- |
| `pages/ComplaintPage.tsx`        | 页面组件 | 投诉页面主体，整合表单、标题、导航等子组件                                   |
| `components/ComplaintHeader.tsx` | 子组件   | 页面标题和说明区域，说明投诉流程和需填信息                                   |
| `components/ComplaintForm.tsx`   | 子组件   | 投诉表单组件，包含类别选择、内容输入、附件上传、联系方式等字段               |
| `components/ComplaintTopNav.tsx` | 子组件   | 页面顶部导航栏，返回按钮、进度指示器等                                       |
| `types/complaint.types.ts`       | 类型文件 | TypeScript类型定义：ComplaintFormData（表单数据）、ComplaintStatus（状态）等 |
| `index.ts`                       | 导出文件 | 统一导出投诉模块                                                             |

**路由**: `/complaint`

---

### 6. **Privacy 隐私政策模块** (`src/features/privacy/`)

**用途**: 隐私政策展示

**核心文件说明：**

| 文件                           | 类型     | 说明                                                       |
| ------------------------------ | -------- | ---------------------------------------------------------- |
| `pages/PrivacyPolicy.tsx`      | 页面组件 | 隐私政策页面主体，整合各分段内容和页脚                     |
| `components/PolicyHeader.tsx`  | 子组件   | 策略页头部，显示标题、最后更新时间、内容导航目录           |
| `components/PolicySection.tsx` | 子组件   | 单个政策分段组件，可复用显示不同政策条款                   |
| `components/PolicyTable.tsx`   | 子组件   | 表格组件，用于展示数据处理、用户权利等结构化信息           |
| `components/Footer.tsx`        | 子组件   | 页脚组件，显示联系信息、修订历史、法律声明等               |
| `components/BackSection.tsx`   | 子组件   | 返回区域，提供返回首页或上级页面的导航按钮                 |
| `data/policyContent.tsx`       | 数据文件 | 隐私政策的文本内容，以结构化数据形式存储，便于维护和国际化 |
| `index.ts`                     | 导出文件 | 统一导出隐私政策模块                                       |

**路由**: `/privacy`

---

### 7. **Terms 使用条款模块** (`src/features/terms/`)

**用途**: 服务使用条款

**核心文件说明：**

| 文件                   | 类型     | 说明                                                     |
| ---------------------- | -------- | -------------------------------------------------------- |
| `pages/TermsOfUse.tsx` | 页面组件 | 使用条款页面，展示服务条款、免责声明、用户义务等法律文件 |
| `index.ts`             | 导出文件 | 统一导出使用条款模块                                     |

**路由**: `/terms`

---

## 🔧 共享层详解 (`src/shared/`)

### Components 通用组件

| 文件                            | 类型     | 说明                                                                |
| ------------------------------- | -------- | ------------------------------------------------------------------- |
| `LanguageSwitcher.tsx`          | 组件     | 多语言切换器，下拉菜单显示可用语言（中文/英文），切换后应用重新渲染 |
| `modals/LogoutConfirmModal.tsx` | 组件     | 登出确认弹窗，用户点击登出时显示确认提示                            |
| `modals/SuccessToastModal.tsx`  | 组件     | 成功操作提示组件，短暂显示成功消息后自动消失                        |
| `modals/AvatarEditorModal.tsx`  | 组件     | 头像编辑弹窗，支持图片上传、裁剪、预览功能                          |
| `menus/UserAvatarMenu.tsx`      | 组件     | 用户头像下拉菜单，点击头像展开用户信息、设置、登出等选项            |
| `index.ts`                      | 导出文件 | 统一导出所有共享组件                                                |

### API 通信层

| 文件         | 类型     | 说明                                                                     |
| ------------ | -------- | ------------------------------------------------------------------------ |
| `config.ts`  | 配置文件 | Axios实例配置：API基础URL、超时时间、拦截器（认证token注入、错误处理等） |
| `chatSSE.ts` | 工具函数 | Server-Sent Events处理，用于聊天消息的实时流推送，实现流式响应解析       |
| `index.ts`   | 导出文件 | 统一导出API模块                                                          |

### i18n 国际化

| 文件                         | 类型     | 说明                                                        |
| ---------------------------- | -------- | ----------------------------------------------------------- |
| `config.ts`                  | 配置文件 | i18next初始化配置：语言检测、默认语言、命名空间、后端连接等 |
| `index.ts`                   | 导出文件 | 统一导出i18n实例                                            |
| `locales/en-US/auth.json`    | 翻译文件 | 英文：认证页面翻译（登录、注册、密码重置相关文本）          |
| `locales/en-US/common.json`  | 翻译文件 | 英文：通用翻译（按钮、菜单、通用术语等）                    |
| `locales/en-US/landing.json` | 翻译文件 | 英文：首页翻译（Hero区域、功能描述等）                      |
| `locales/zh-CN/auth.json`    | 翻译文件 | 中文：认证页面翻译                                          |
| `locales/zh-CN/common.json`  | 翻译文件 | 中文：通用翻译                                              |
| `locales/zh-CN/landing.json` | 翻译文件 | 中文：首页翻译                                              |

**支持语言**: 简体中文 (zh-CN) 和 英文 (en-US)

### Types 通用类型定义

| 文件            | 类型     | 说明                                                                                |
| --------------- | -------- | ----------------------------------------------------------------------------------- |
| `user.types.ts` | 类型文件 | 用户相关接口定义：UserInfo（用户信息）、UserProfile（用户资料）、UserRole（角色）等 |
| `index.ts`      | 导出文件 | 统一导出所有类型定义                                                                |

### Utils 工具函数

| 文件       | 类型     | 说明                                                            |
| ---------- | -------- | --------------------------------------------------------------- |
| `jwt.ts`   | 工具函数 | JWT令牌处理：解析token、提取声明信息、验证过期时间、刷新token等 |
| `env.ts`   | 工具函数 | 环境变量读取和验证，确保必要的环境变量已配置                    |
| `index.ts` | 导出文件 | 统一导出所有工具函数                                            |

### Data 静态数据

| 文件              | 类型     | 说明                                                 |
| ----------------- | -------- | ---------------------------------------------------- |
| `projectIntro.ts` | 数据文件 | 项目介绍数据，包含产品特性、优势、使用统计等展示内容 |

---

## 🏗️ 分层架构

```
┌─────────────────────────────────┐
│         页面层 (Pages)           │  ← 路由目标
├─────────────────────────────────┤
│      功能组件层 (Components)     │  ← 功能实现
├─────────────────────────────────┤
│   功能层 (Features) + 共享层     │  ← 业务逻辑
│       (Shared Resources)        │
├─────────────────────────────────┤
│     API + Utils + Types         │  ← 基础设施
└─────────────────────────────────┘
```

### 关键设计原则

✅ **功能导向结构 (Feature-Based)**

- 相关代码紧密组织在一个feature目录
- 便于功能维护和独立开发

✅ **单一职责**

- `pages/` - 页面级组件，处理路由
- `components/` - 可复用UI组件
- `types/` - 类型定义
- `data/` - 静态数据

✅ **统一导出**

- 每个模块提供 `index.ts` 统一导出
- 简化导入路径

✅ **私有与共享**

- 模块内 `components/` 是私有的
- `shared/` 下的组件可跨模块使用

✅ **类型安全**

- 全部使用 TypeScript
- 模块级类型定义

---

## 📝 NPM脚本

| 脚本      | 命令                   | 说明                 |
| --------- | ---------------------- | -------------------- |
| `dev`     | `vite`                 | 启动开发服务器       |
| `build`   | `tsc -b && vite build` | 编译TypeScript并构建 |
| `lint`    | `eslint .`             | 代码检查             |
| `format`  | `prettier --write .`   | 代码格式化           |
| `preview` | `vite preview`         | 预览生产构建         |

---

## 📝 使用规范

### 1. 导入规范

#### ✅ 推荐：使用统一导出

```typescript
// 从feature导入
import { Login, Register, AuthLayout } from '@/features/auth';

// 从shared导入
import { LogoutConfirmModal, UserAvatarMenu } from '@/shared/components';
import { parseJwt } from '@/shared/utils';
import type { UserInfo } from '@/shared/types';
```

#### ❌ 避免：直接导入内部文件

```typescript
// 不推荐
import Login from '@/features/auth/pages/Login';
```

### 2. 添加新功能

创建新功能模块时：

```bash
# 1. 创建目录结构
mkdir -p src/features/new-feature/{pages,components,types}

# 2. 创建index.ts导出文件
echo "export { default as NewFeature } from './pages/NewFeature';" > src/features/new-feature/index.ts

# 3. 在App.tsx中添加路由
# 4. 在相应页面导入使用
```

### 3. 添加共享组件

```bash
# 1. 创建组件
src/shared/components/[category]/NewComponent.tsx

# 2. 在shared/components/index.ts中导出
export { default as NewComponent } from './[category]/NewComponent';
```

---

## 🚀 快速导航

- **新增功能页面** → 在 `src/features/` 下创建新feature目录
- **添加通用组件** → `src/shared/components/`
- **API调用** → 使用 `src/shared/api/`
- **国际化** → 修改 `src/shared/i18n/locales/`
- **全局样式** → `src/app/styles/index.css`

---

**最后更新**: 2026年1月28日
